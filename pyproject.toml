[project]
name = "soccer-caster"
version = "0.1.0"
description = "Broadcast soccer to post-game summary, and summary to live commentary"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src", "tests"]
testpaths = ["tests"]
filterwarnings = ["error::DeprecationWarning"]
