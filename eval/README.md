Hand-labelled ground truth lives here.

Label events on two or three full matches and measure precision/recall per
event type. Without this the pipeline cannot be tuned, and the live caster
will confidently narrate garbage.
